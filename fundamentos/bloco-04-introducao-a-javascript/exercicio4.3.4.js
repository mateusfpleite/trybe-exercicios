maiorPrimo = 0; 
numeros = [];
primos = [];
for (let index = 0; index < 51; index+=1) {
    numeros.push(index)
    for (numbers of numeros) {
            if (numeros[index]%numeros[index]+2 >1) {
                primos.push(numeros[index])
        }
     }
}
console.log(primos)